version: "3.9"
services:
  blito-backend:
    build: .
    container_name: blito-backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./core:/app
    ports:
      - "12000:8000"
    environment:
      - SECRET_KEY=test
      - DEBUG=True
